# OPCIONAL. Por padrão o endereço do preview (og:image) é detectado pelo domínio
# pelo qual o site foi aberto (Host / X-Forwarded-Host / X-Forwarded-Proto).
# Defina só se quiser fixar o endereço. Com ou sem https:// (sem, assume https).
NEXT_PUBLIC_SITE_URL=https://seu-dominio.com.br
